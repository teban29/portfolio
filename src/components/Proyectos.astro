<section id="proyectos" class="relative bg-surface py-24 sm:py-32">
  <div class="mx-auto max-w-6xl px-6">
    <div class="mb-16 lg:mb-20">
      <p class="mb-3 font-mono text-xs font-medium tracking-wide text-primary">
        Proyectos
      </p>
      <h2 class="text-balance font-sans text-[clamp(2rem,4vw,3rem)] font-bold leading-[1.12] tracking-[-0.02em] text-ink">
        Trabajo real
      </h2>
      <p class="mt-4 max-w-md text-base leading-relaxed text-muted">
        Proyectos propios y colaboraciones donde aporté como desarrollador full-stack.
      </p>
    </div>

    <div class="grid gap-6 sm:grid-cols-2">
      {
        [
          {
            num: "01",
            title: "Deb Tout",
            url: "https://deb-tout.com/",
            role: "propio",
            roleLabel: "Proyecto propio",
            desc: "Landing page diseñada y desarrollada con Astro. Optimizada para rendimiento, accesibilidad y una experiencia de navegación fluida.",
            tags: ["Astro", "Tailwind CSS"],
            status: null,
            company: null,
            companyUrl: null,
            image: "/mockups/debtoutscorps.png",
            mockup: "deb-tout",
          },
          {
            num: "02",
            title: "AqueTeVienes e-commerce",
            url: null,
            role: "propio",
            roleLabel: "Proyecto propio",
            desc: "Plataforma e-commerce completa para 'AqueTeVienes' . Backend con Django REST Framework, frontend con Next.js. Panel de administración, gestión de inventario y pasarela de pagos.",
            tags: ["Django REST", "Next.js", "TypeScript"],
            status: "En desarrollo",
            company: null,
            companyUrl: null,
            image: null,
            mockup: "aquete-vienes",
          },
          {
            num: "03",
            title: "MotoHuevo",
            url: "https://motohuevo.com/",
            role: "participacion",
            roleLabel: "Desarrollador full-stack",
            desc: "Marketplace de repuestos. Desarrollé módulos de mensajeros urbanos, endpoints para la app mobile e integraciones con APIs de Google.",
            tags: ["Laravel", "PHP", "Google APIs"],
            status: null,
            company: "Sohe Solutions",
            companyUrl: "https://sohesolutions.com.co/",
            contribution: "Módulos de mensajería, API endpoints, integraciones Google",
            image: "/mockups/motohuevo.png",
            mockup: "motohuevo",
          },
          {
            num: "04",
            title: "ProfessionalHubs",
            url: "https://professionalhubs.com/",
            role: "participacion",
            roleLabel: "Desarrollador full-stack",
            desc: "Plataforma de networking profesional. Participé en requerimientos e integraciones específicas como parte del equipo de desarrollo.",
            tags: ["Strapi", "Next.js", "TypeScript"],
            status: null,
            company: "Sohe Solutions",
            companyUrl: "https://sohesolutions.com.co/",
            contribution: "Requerimientos, integraciones, desarrollo full-stack",
            image: "/mockups/professionalhubs.png",
            mockup: "professional-hubs",
          },
          {
            num: "05",
            title: "Arkanet AI",
            url: "https://arkanetai.com/",
            role: "participacion",
            roleLabel: "Desarrollador",
            desc: "Proyectos en desarrollo activo usando Go para servicios backend de alto rendimiento y Next.js para el frontend.",
            tags: ["Go", "Next.js", "TypeScript"],
            status: "En desarrollo",
            company: "Arkanet AI",
            companyUrl: "https://arkanetai.com/",
            contribution: "Desarrollo full-stack con Go y Next.js",
            image: null,
            mockup: "arkanet-ai",
          },
        ].map((project) => (
          <div class={`project-card group rounded-2xl border border-border/50 bg-bg p-3 transition-all duration-300 hover:border-border hover:shadow-sm ${project.num === "01" ? "sm:col-span-2" : ""}`}>
            <div class="relative mb-4 overflow-hidden rounded-xl aspect-[16/9]">
              {
                project.image ? (
                  <img
                    src={project.image}
                    alt={`Preview de ${project.title}`}
                    class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.02]"
                    loading="lazy"
                  />
                ) : (
                  <div class={`mockup-fallback mockup-${project.mockup} h-full w-full`}>
                    <div class="absolute inset-0 flex items-center justify-center">
                      <span class="font-mono text-2xl font-bold tracking-wider text-white/15 select-none">
                        {project.title.charAt(0)}{project.title.charAt(project.title.length - 1)}
                      </span>
                    </div>
                  </div>
                )
              }
              <div class="absolute inset-0 rounded-xl ring-1 ring-inset ring-white/[0.04]"></div>
            </div>

            <div class="space-y-3 px-1 pb-2">
              <div class="flex flex-wrap items-center gap-2">
                <span class="font-mono text-xs font-medium tabular-nums text-primary/50">
                  {project.num}
                </span>
                {project.role === "propio" ? (
                  <span class="rounded-full bg-primary/10 px-2 py-0.5 font-mono text-[10px] font-semibold leading-relaxed text-primary">
                    {project.roleLabel}
                  </span>
                ) : (
                  <span class="inline-flex items-center gap-1 rounded-full border border-border/60 px-2 py-0.5 font-mono text-[10px] font-medium leading-relaxed text-muted">
                    Participación
                    {project.companyUrl && <span class="text-muted/40">·</span>}
                    {project.companyUrl ? (
                      <a href={project.companyUrl} target="_blank" rel="noopener noreferrer" class="font-semibold text-ink/70 transition-colors hover:text-ink">{project.company}</a>
                    ) : (
                      <span class="font-semibold text-ink/70">{project.company}</span>
                    )}
                  </span>
                )}
                {project.status && (
                  <span class="rounded-full bg-accent/10 px-2 py-0.5 font-mono text-[10px] font-medium leading-relaxed text-accent">
                    {project.status}
                  </span>
                )}
              </div>

              <h3 class="font-sans text-lg font-semibold leading-snug text-ink">
                {project.url ? (
                  <a href={project.url} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1.5 transition-colors duration-200 hover:text-primary">
                    {project.title}
                    <svg class="h-3.5 w-3.5 shrink-0 opacity-0 transition-all duration-200 group-hover:opacity-40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
                      <polyline points="15 3 21 3 21 9" />
                      <line x1="10" y1="14" x2="21" y2="3" />
                    </svg>
                  </a>
                ) : (
                  project.title
                )}
              </h3>

              <p class="text-sm leading-relaxed text-muted">
                {project.desc}
              </p>

              {project.contribution && (
                <p class="text-xs leading-relaxed text-muted/60">
                  {project.contribution}
                </p>
              )}

              <div class="flex flex-wrap gap-1.5 pt-1">
                {project.tags.map((tag: string) => (
                  <span class="rounded-full border border-border/50 bg-surface px-2 py-0.5 font-mono text-[10px] font-medium leading-relaxed text-muted">
                    {tag}
                  </span>
                ))}
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<script>
  function initReveal() {
    const section = document.getElementById("proyectos");
    if (!section) return;

    const items = section.querySelectorAll(".project-card");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("is-visible");
          }
        });
      },
      { threshold: 0.1, rootMargin: "0px 0px -40px 0px" },
    );

    items.forEach((item) => {
      observer.observe(item);
    });
  }

  initReveal();
</script>

<style is:inline>
  .project-card {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.5s ease, transform 0.5s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .project-card.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .project-card:nth-child(1) { transition-delay: 0.05s; }
  .project-card:nth-child(2) { transition-delay: 0.1s; }
  .project-card:nth-child(3) { transition-delay: 0.15s; }
  .project-card:nth-child(4) { transition-delay: 0.2s; }
  .project-card:nth-child(5) { transition-delay: 0.25s; }

  .mockup-fallback {
    position: relative;
    overflow: hidden;
  }

  .mockup-deb-tout {
    background:
      linear-gradient(135deg, oklch(0.400 0.160 275) 0%, oklch(0.580 0.185 20) 100%);
  }
  .mockup-deb-tout::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      repeating-linear-gradient(
        0deg,
        transparent,
        transparent 8%,
        oklch(1 0 0 / 0.03) 8%,
        oklch(1 0 0 / 0.03) 8.5%
      ),
      linear-gradient(180deg, oklch(0 0 0 / 0.3) 0%, transparent 40%);
  }

  .mockup-aquete-vienes {
    background:
      linear-gradient(150deg, oklch(0.300 0.100 320) 0%, oklch(0.450 0.170 275) 100%);
  }
  .mockup-aquete-vienes::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 30% 35%, oklch(1 0 0 / 0.08) 0%, transparent 60%),
      radial-gradient(circle at 70% 65%, oklch(1 0 0 / 0.05) 0%, transparent 50%);
    border-radius: inherit;
  }

  .mockup-motohuevo {
    background:
      linear-gradient(135deg, oklch(0.450 0.200 25) 0%, oklch(0.550 0.220 40) 100%);
  }
  .mockup-motohuevo::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      repeating-linear-gradient(
        -30deg,
        transparent,
        transparent 30px,
        oklch(1 0 0 / 0.04) 30px,
        oklch(1 0 0 / 0.04) 32px
      );
  }

  .mockup-professional-hubs {
    background:
      linear-gradient(135deg, oklch(0.350 0.120 255) 0%, oklch(0.500 0.160 275) 100%);
  }
  .mockup-professional-hubs::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(0deg, oklch(1 0 0 / 0.04) 1px, transparent 1px),
      linear-gradient(90deg, oklch(1 0 0 / 0.04) 1px, transparent 1px);
    background-size: 32px 32px;
  }

  .mockup-arkanet-ai {
    background:
      linear-gradient(160deg, oklch(0.100 0.015 260) 0%, oklch(0.180 0.060 275) 100%);
  }
  .mockup-arkanet-ai::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: radial-gradient(oklch(0.600 0.180 275 / 0.12) 1.5px, transparent 1.5px);
    background-size: 24px 24px;
  }

  @media (prefers-reduced-motion: reduce) {
    .project-card {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>
